[04/17/25]
Adding this file randomly, bc I forgot to make one when I started this repo LMFAO!!

Levitate is my idea for a truly unique text editor, stemming from a single thought: "What if I could click anywhere on the screen, and start typing?".

[04/18/25]
Let's get some abstract ideas and a general framework down. I think a good idea would be to have a state machine for things like being in writing mode (i.e: does the text field have focus) or (if I implement it) Vim-esque modes like "command mode". I also think that isolating the text-rendering functionality right off the bat is a good idea, so I can implement a basic console printout in place of font rendering while I'm setting up/testing everything else (because lord knows that font rendering is gonna take the most time). All right, let's get started with a basic app, window, and console printout functionality (maybe even a text buffer?). Also, try to avoid using game-engine concepts where they aren't needed (mainly, don't just immediately make a main loop).

I'm getting so sidetracked and distracted; first thing's first: get user input and print it to the terminal as it's being typed.

Okay, I have a very very very very VERY bare bones way of getting input and sending it to `std::cout` via GLFW. Now, time to make a function for handling this.

Basic input and output with even more basic backspacing is done! Time to... implement fonts.

HUGE IDEA FOR A FEATURE: Drag and drop files into Levitate to pin them! A small window containing your file's data (represented as text) will be dropped into your current Levitate project, and can be dragged around, rotated, or even resized. The window will have very faint/thin borders and will take mouse/keyboard input, giving off the illusion that it's a magic portal into another Levitate file (which it kinda is).

Todo: clean up everything and start working on an actual prototype.

[04/20/15]
At work from 8am-4pm today; I've spent an hour and a half creating a custom `vec3` type for fun (and practice). Just trying my hand at creating a generic math type that is compatible/interchangeable with things like `float*`, `std::vector<float>`, `glm::vec3`, etc.

While making this type, I had an idea: what if I could write my own implementation of certain operators for Jolt's math types to convert them to glm types without needing to use an external function? I wanna try that...

Anyways, back to what I was doing.

I really like the structure of my code, but I don't want to fall into unnecessary abstraction and/or organization, so I'm gonna reign it in a lil bit lmfao.

[06/10/25]
I want to build this into a library and bundle it into Nostalgia as the text editor for the UI (or at least bundle part of it as a library, the rest will be the app "Levitate"). I need to refactor the project to better match my new found knowledge on project structure and I need a working build before I can do that, so I might revert everything back to the commit on 04/18/25, since that's the last working build of Levitate; I'll have to look at the changes I made and decide whether or not it's worth it to do so or to just fix the main issue (that being the shader compilation iirc).

Dude..... the fucking errors had nothing to do with the compiling. They weren't errors. I was incorrectly using 'glGetShaderiv' instead of 'glGetProgramiv' for checking program linking errors.....

SDKLFJSKLDFJSDKLFJKLSDFJKLJSDKLJSDJFJSDKLFJSDKLFJWEIOHJJKL

I think I've been using std::cout very wrong and badly and it's fucking with all the string streams or something to that effect.